# StyleWarehouse Robot Navigation — Python dependencies
# Core simulation runs with zero third-party dependencies.
# pygame is optional (only needed for animated GUI visualisation).

# Optional visualisation
pygame>=2.5.0   # pip install pygame

# Optional API layer (stretch goal)
# fastapi>=0.110.0
# uvicorn>=0.27.0
