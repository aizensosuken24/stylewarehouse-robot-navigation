# StyleWarehouse Robot Navigation — deployment dependencies
# pygame is removed: it requires SDL which isn't available on cloud servers.
# Install pygame locally only: pip install pygame

# HTTP helpers
requests>=2.28.0
python-dotenv>=1.0.0

# Deployment server (Render uses gunicorn)
gunicorn>=23.0.0
